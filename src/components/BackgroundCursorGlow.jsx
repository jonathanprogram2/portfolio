import React, { useEffect, useRef } from "react";

export default function BackgroundCursorGlow() {
    // const orbRef = useRef(null);

    // useEffect(() => {
    //     const orb = orbRef.current;

    //     const moveOrb = (e) => {
    //         orb.style.left = `${e.clientX}px`;
    //         orb.style.top = `${e.clientY}px`;
    //     };

    //     window.addEventListener("mousemove", moveOrb);
    //     return () => window.removeEventListener("mousemove", moveOrb);
    // }, []);

    // return (
    //     <div
    //         ref={orbRef}
    //         className="fixed w-72 h-72 bg-[#FF4EBD]/20 rounded-full pointer-events-none blur-[120px] z-0 transition-transform duration-75 ease-out"
    //         style={{
    //             left: 0,
    //             top: 0,
    //             transform: "translate(-50%, -50%)",
    //             mixBlendMode: "screen",
    
    //         }}
    //     />
    // );   
}